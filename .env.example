# Database connection (PostgreSQL for production)
# Get free DB from: https://neon.tech
DATABASE_URL="postgresql://username:password@host/dbname?sslmode=require"

# Anthropic API Key for Claude PDF extraction
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY="sk-ant-..."

# Vercel Blob Storage Token (auto-provided by Vercel when you add Blob storage)
BLOB_READ_WRITE_TOKEN="vercel_blob_..."

# Secret to protect the /api/process-pdfs cron endpoint
# Generate any random string: openssl rand -hex 32
CRON_SECRET="your-random-secret-here"
